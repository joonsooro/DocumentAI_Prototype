# S2 — Contract

Spec → contract.

---

## Stages

1. Run — Contract assembly vs. [`./contract-blueprint.md`](./contract-blueprint.md) reading spec + tight-thesis. One question per screen, then a dependencies / credentials / skills interview (API keys, design-system pointer, named Claude skills like `frontend-design`) → `contract.html`
2. Close — append to `claude-progress.txt`

---

## Build chain

| Artifact | Path                |
|----------|---------------------|
| Contract | `app/contract.html` |
