# S4 — Evaluate

App + contract → eval verdict per pick.

---

## Stages

1. Run — Independent Judge vs. [`./eval-blueprint.md`](./eval-blueprint.md) reading contract + app + progress → `eval-results.html`
2. Close — (runs as the blueprint's final step) append to `claude-progress.txt` and update `CLAUDE.md` §4 Eval Bar

---

## Build chain

| Artifact          | Path                                               |
|-------------------|----------------------------------------------------|
| Eval results      | `app/eval-results.html`                            |
| Trace (appended)  | `app/claude-progress.txt`                          |
