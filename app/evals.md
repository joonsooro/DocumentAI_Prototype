# evals.md — Document AI Self-Improvement Flywheel (S1)

Binary pass/fail cases harvested 1:1 from `app/spec.html` §3. 12 rows: 6 HAPPY · 4 EDGE · 2 RED.

No 1–5 scoring. No LLM-as-judge. Assertions check shape, presence, and refusal patterns — not exact agent response text.

| Case ID | Scenario | Expected output | Assertion | FAQ source |
|---|---|---|---|---|
| HAPPY-1 | A new operator opens the prototype with no prior context. | Customer Workspace renders the admin-facing intent form and a labelled `CustomerIntent` panel; no end-user upload affordance is present. | `screen.getByLabelText(/customer intent/i)` exists; no `data-testid="end-user-upload"` element rendered; intent flow targets `role=admin`. | HAPPY-1 |
| HAPPY-2 | Admin pastes the DAEJOO prose requirement (supplier, PO, HS code, payment terms, payable amount, exclude no-commercial-value sample lines). | Live AI Core compile produces a `CompiledConfiguration` with all 9 schema fields, field instructions, validation rules, regex patterns, confidence thresholds, processing mode. | `result.schema.fields.length === 9`; every field has non-empty `instruction`, `validation`, `regex`, `confidenceThreshold`; `result.processingMode` is set. | HAPPY-2, HAPPY-3 |
| HAPPY-3 | Same DAEJOO prose; observe agent type for the compile step. | Compile result is tagged as a live `aiCore` agent call, not a template. | `result.source === 'aiCore'`; `result.templateUsed === false`; `QualityMetric` log includes one `aiCore.compile` entry. | HAPPY-3 |
| HAPPY-4 | Admin reviews capability assessment for the DAEJOO requirement set. | Supplier/PO/invoice date → `Supported`; no-commercial-value exclusion → `Supported with workaround`; material-disposal phrase never appears in this view; that phrase appears in Internal as `unsupported_free_text_business_condition`. | Customer Workspace HTML contains no string `Unsupported`; no string `material disposal`; Internal Workspace contains a `ProductSignal` whose `signal_type === 'unsupported_free_text_business_condition'`. | HAPPY-4, RED-2 |
| HAPPY-5 | Admin opens readiness panel after the DAEJOO run. | Readiness panel shows business-language reasons (field, evidence, rule/config, confidence, recommended next action); no raw prompt text; no raw model output. | every reason object has the 5 keys `field, evidence, rule, confidence, nextAction`; panel HTML does not contain the substrings `system:` or `prompt:` or `<|`. | HAPPY-5 |
| HAPPY-6 | Reviewer inspects the three-workspace separation. | Customer / Admin / Internal exist as distinct routes; each reads the same domain objects but renders its own concern. | three routes mount; shared `useDomainStore()` returns the same root object reference in all three; no workspace renders another's data-testid. | HAPPY-6 |
| EDGE-1 | DAEJOO run completes but `payable amount` is missing from extraction. | A `ClarificationRequest` is generated asking field meaning, posting/review/reporting impact, and supplier-scope applicability; no default value substituted. | `clarifications.length >= 1`; the request for `payable_amount` has the 3 prompts; `documentRun.fields.payable_amount` is `null` not a default. | EDGE-1 |
| EDGE-2 | AI Core call for `generateAdminRecommendations` throws / times out / returns invalid JSON. | UI surfaces a `ClarificationRequest` with an operator-facing error; a `QualityMetric` is logged; no canned recommendation appears. | `qualityMetrics` includes `{agent:'admin.recommend', status:'fail'}`; UI shows an error-tagged `ClarificationRequest`; admin recommendation list does not contain any item with `source==='fallback'`. | EDGE-2 |
| EDGE-3 | One operator submits a single correction for a non-recurring DAEJOO field. | No `ProductSignal` is auto-created; the correction sits in the governance queue with frequency/customer-impact/document-type/supplier/country fields pending. | `productSignals` length unchanged after the single correction; `governanceQueue` includes the new `CorrectionEvent` with the 5 governance fields present (some may be unset, none auto-promoted). | EDGE-3 |
| EDGE-4 | Operator walks the full demo (Customer → Admin → Internal) timed end-to-end. | Walkthrough completes in under 5 minutes; all 7 eval families execute against the DAEJOO scenario; adding a second supplier fixture in `src/data/` does not require editing the control layer. | scripted walkthrough Vitest scenario completes with `walkthroughDurationMs < 300_000`; the 7 eval families all report a pass count; a stub Amazon fixture loads without modifying `src/domain/*`. | EDGE-4 |
| RED-1 | Admin opens recommendations after a DAEJOO run that left one field below auto-confirm. | Recommendations list contains schema/prompt/instruction recommendations only; no entry suggests lowering a threshold; readiness for that field stays `Needs review`. | for every `AdminRecommendation r`: `r.type !== 'threshold_lower'` and `r.text` does not match `/lower(ing)?\s+threshold/i`; readiness for the under-confirm field is `Needs review`, not `Ready`. | RED-1 |
| RED-2 | Stakeholder asks the demo to "tell the customer the material-disposal phrase is unsupported." | Customer Workspace continues to show only `Supported` / `Supported with workaround` and the clarification loop; the material-disposal phrase remains only inside Internal Product Intelligence. | full-DOM text snapshot of Customer Workspace does not contain `Unsupported`, `material disposal`, or `unsupported_free_text_business_condition`; same strings DO appear inside Internal Workspace DOM. | RED-2 |

Distribution check: 6 HAPPY (HAPPY-1…6) · 4 EDGE (EDGE-1…4) · 2 RED (RED-1, RED-2) = 12 total, 1:1 with spec §3.

---

## S1 amendment pass (2026-05-26) — 10 new cases

Appended for the conversational chat subsystem (R11/R12), roadmap-ranking formula (R6), threshold-decrease informational guard (R3), Readiness stub actions (R10), and N9 enforcement. Cases below are additive — existing 12 cases (HAPPY-1…6, EDGE-1…4, RED-1, RED-2) are unchanged.

| Case ID | Scenario | Expected output | Assertion | FAQ source |
|---|---|---|---|---|
| HAPPY-7 | A fixture ProductSignal carries `frequency=17, customerCount=4, workaroundBurden='high', actionability='high', expectedStpLift=30`. | `computeRoadmapScore(signal)` is deterministic and returns exactly 91. | `computeRoadmapScore(signal) === 91` across 10 sequential invocations; no variance across runs. | HAPPY-7, A11 |
| HAPPY-8 | 4 fixture ProductSignals matching the design handoff Roadmap rows (#1 free-text obligation, #2 commercial-value handling, #3 supplier-pinned prompt, #4 net-days normalization). | `rankRoadmapEvidence(signals)` returns the 4 signals in exactly that order. | `ranked.map(s => s.id)` equals `[#1, #2, #3, #4]` across 10 sequential invocations; no swaps. | HAPPY-7, A11 |
| HAPPY-9 | The same #1 fixture from HAPPY-8 (`frequency=17, customerCount=4, workaroundBurden='high', actionability='high', expectedStpLift=30`). | The reason line generator returns the exact string `"17 occurrences · 4 customers · workaround-heavy · directly addressable"`. | `generateRoadmapReason(signal) === "17 occurrences · 4 customers · workaround-heavy · directly addressable"`; byte-for-byte equality. | HAPPY-7, A11 |
| HAPPY-10 | A Customer Readiness step rendered with `status='Needs review'`. Operator clicks 'Confirm & process'; separately operator clicks 'Save as draft'. | Both actions surface a confirmation toast; readiness verdict unchanged; no downstream entity created. | post-click `readiness.status === 'Needs review'`; `getCorrections().length`, `getProductSignals().length`, `getMetrics().length` all unchanged vs. pre-click snapshot; toast element present in DOM. | HAPPY-5, D3 |
| HAPPY-11 | User submits an initial intent missing `payment_terms` context. Assistant emits a `clarification_question` turn. User replies with payment_terms detail. | `chat.turn_decide` returns `{action:'recompile'}`; F-04 re-compiles with prior chat context appended; CompiledConfigPanel updates in-place; readiness panel renders with all 9 fields populated; no notify-team question appears. | `ConversationState.turns` contains ≥2 user turns, ≥1 `recompile_announcement`, ≥1 `success_summary`; `compiledConfigVersionRefs.length >= 2`; final readiness status is `Ready` or `Needs review` with no missed fields; no turn with `kind==='notify_team_question'`. | HAPPY-11, A12 |
| HAPPY-12 | User submits an intent containing a capability-class pattern (e.g. `"Can you fill these fields in S/4 HANA?"`). | `chat.turn_decide` classifies the turn as `capability_class`; conversation ends with a `notify_team_question` bubble; user replies 'yes'; one ProductSignal is created with `status='provisional'`, `provenance='conversational_notify_team'`. | turns array ends with `notify_team_question` then user 'yes' then `notify_team_confirmation`; `getProductSignals().filter(s => s.status==='provisional' && s.provenance==='conversational_notify_team').length === 1`; the new signal's `governanceApprovedAt === null`. | HAPPY-12, A13, A14 |
| EDGE-5 | Admin attempts to set a threshold lower than the current value via the Admin Threshold Governance UI. | UI surfaces the dual-approval requirement (direction tag `↓ requires approval`); no mutation to `app-spec.json` or the in-memory threshold store; no entity created. The dual-approval workflow itself is OQ-4 and out of scope for this case. | post-attempt threshold store unchanged; rendered DOM contains `↓ requires approval`; no `CorrectionEvent` / `ProductSignal` / `QualityMetric` created. | RED-1 (extended), A10 |
| EDGE-6 | User submits a capability-class request. Conversation ends with `notify_team_question`. User replies 'no'. | ConversationState enters `completed`; no ProductSignal created; /internal RoadmapSignalsPanel unchanged. | post-decline `getProductSignals().length` unchanged vs. pre-conversation snapshot; conversation status is `completed`; no DOM mutation in /internal RoadmapSignalsPanel. | EDGE-6, N9 |
| EDGE-7 | User submits intent + clarifications that exercise only the missed-extraction path (no capability-class patterns). | After successful re-compile and full extraction, conversation status transitions to `success`; no `notify_team_question` bubble; no ProductSignal created. | turns array contains no turn with `kind==='notify_team_question'`; conversation status is `success`; `getProductSignals()` length unchanged. | EDGE-7, A14 |
| RED-3 | Force the conversation through a sequence where a code path attempts to skip the `notify_team_question` and write a ProductSignal directly (simulate via direct call to the signal-write layer). | The signal-write layer rejects the write; no ProductSignal lands in the store; N9 invariant enforced at the data layer, not just at the chat UX layer. | direct `_writeProvisionalSignal(...)` without `ConversationState.status === 'awaiting_notify_decision'` + user='yes' guard returns refusal; `getProductSignals()` length unchanged; assertion checks both the function-level guard and the resulting store snapshot. | RED-3, N9 |

Post-amendment distribution: 12 (original) + 10 (S1 amendment) = 22 total. Original 12 cases retain 1:1 mapping with spec §3 FAQ rows HAPPY-1..6 / EDGE-1..4 / RED-1, RED-2; the 10 new cases map to spec §3 FAQ rows HAPPY-7, HAPPY-11, HAPPY-12, EDGE-6, EDGE-7, RED-3 plus extensions of HAPPY-5 and RED-1.

---

## S1 additive pass (2026-05-27) — 1 new case (Agent I/O Log)

Appended for the new Agent I/O Log screen (A16 / HAPPY-13). Additive only — existing 22 cases are unchanged.

| Case ID | Scenario | Expected output | Assertion | FAQ source |
|---|---|---|---|---|
| HAPPY-13 | Operator opens the Admin Control Plane (or Internal Product Intelligence) after a session that produced ≥1 successful and ≥1 failed agent run. They click the visible "Agent I/O Log" entry point. | The Agent I/O Log panel mounts; it renders one row per `qualityMetricLog` entry (newest first) carrying agent, model, status, latency_ms, tokens_in, tokens_out, timestamp, redacted input-shape summary, redacted output-shape summary, and (for failure rows) `AgentFailure.reason`. The panel is reachable from `/admin` and `/internal` but NOT from `/customer`. The DOM never contains raw prompt text, service-key strings, or the forbidden substrings. | `screen.getByTestId('agent-io-log-panel')` exists on /admin and /internal routes; `panel.querySelectorAll('[data-testid^="agent-io-log-row-"]').length === getMetrics().length`; for every row: `row.textContent` matches the 9-field shape (agent · model · status · latency_ms · tokens_in · tokens_out · timestamp · input-shape · output-shape); for failure rows: `row.textContent` includes one of the 9 `AgentFailure.reason` enum values; full-DOM text snapshot of the panel does NOT contain `system:`, `prompt:`, `<\|`, `AICORE_KEY_PATH`, `clientsecret`, or `material disposal`; on /customer the same `getByTestId('agent-io-log-panel')` query returns null. | HAPPY-13, A16 |

Post-additive distribution: 22 (prior) + 1 (S1 additive) = 23 total. The new case maps 1:1 with new spec §3 FAQ row HAPPY-13; the per-FAQ-row 1:1 eval invariant continues to hold (19 FAQ rows in §3, 19 FAQ rows that have at least one bound eval case, plus the multi-case rows from HAPPY-7 / HAPPY-11 / HAPPY-12 / RED-1 / HAPPY-5 already documented in the S1 amendment pass above).

---

## S1 Cycle 1 pass (2026-05-28) — 5 new cases (Merged Compile Agent re-derivation)

Appended for the Merged Compile Agent (A17), Generated Extraction Prompt (A18), A2 capability-grounding amendment, and the D6 ProductSignal consent flow. Additive only — existing 23 cases are unchanged.

| Case ID | Scenario | Expected output | Assertion | FAQ source |
|---|---|---|---|---|
| HAPPY-14 | user's actual failing transcript → `action='compile'` → 8-9 field schema produced. | The Merged Compile Agent returns `{action: 'compile', payload: {schema, processingMode, extractionSystemPrompt}}` for the user's first-turn field-enumeration transcript; the schema carries 8–9 fields covering supplier/invoice number/PO/invoice date/total/currency/tax/supplier branch; agent NEVER asks the user to share a file. | `decision.action === 'compile'`; `decision.payload.schema.fields.length` is in `[8, 9]`; `typeof decision.payload.extractionSystemPrompt === 'string'` and length > 0; full chat-turn text never matches `/share\s+(the\s+)?(file|document|invoice|image)/i` or `/(could|please)\s+(you\s+)?(attach|upload|provide)/i`. | HAPPY-3, HAPPY-11 |
| HAPPY-15 | user asks to integrate with S/4 HANA → `action='capability_class_question'` → payload includes citation to capability surface. | Agent returns `{action: 'capability_class_question', payload: {confirmationQuestion, capabilityGapDescription, capabilitySurfaceCitation, pendingSignalDescription}}`; capabilitySurfaceCitation names a section of `docs/document-ai-capability-surface.md`. | `decision.action === 'capability_class_question'`; all 4 payload keys present and non-empty strings; `decision.payload.capabilitySurfaceCitation` matches `/p\.\s*\d+(-\d+)?|Service Plans|Boundaries|Integration|Unsupported|Vocabulary|Capabilities/`; `decision.payload.confirmationQuestion` matches `/S\/4\s*HANA/i`. | HAPPY-12, A2 |
| HAPPY-16 | user asks for missing field `supplier_branch` → schema includes it → extraction returns value=null → ExtractedFieldsPanel shows "Not found in this document". | Recompile decision adds `supplier_branch` to schema; `simulateDocumentRun` returns `{value: null, confidence: 0}` for that field (truly missing, not below-threshold); ExtractedFieldsPanel renders the truly-missing marker `Not found in this document` (distinct title attribute from below-threshold). | `decision.payload.schema.fields.some(f => f.name === 'supplier_branch')`; `documentRun.fields.supplier_branch.value === null && documentRun.fields.supplier_branch.confidence === 0`; the rendered row for `supplier_branch` has title attr / textContent `Not found in this document`; below-threshold rows render a different title string (does NOT contain `Not found in this document`). | HAPPY-11, A7 |
| HAPPY-17 | user asks to see the generated prompt → chat renders `prompt_display` bubble. | Chat appends a turn with `kind === 'prompt_display'` whose `content` is the live-generated `CompiledConfiguration.extractionSystemPrompt`; bubble renders in monospace; the displayed prompt is the A18 extraction prompt, NOT an A17/A1/A5/A7 agent system prompt. | a `ChatTurn` with `kind === 'prompt_display'` exists; its `content === vm.configuration.extractionSystemPrompt`; the bubble DOM has `font-family: monospace` (or equivalent token); full bubble text does not contain `system:` or `<|`. | HAPPY-3, A18 |
| HAPPY-18 | after capability_class_question, user says yes → ProductSignal is written. | After agent emits `capability_class_question`, `ConversationState.pendingSignal` is set and `status === 'awaiting_notify_decision'`; on explicit user 'yes', `governProductSignals` writes a single `ProductSignal` with `status: 'provisional'` + `provenance: 'conversational_notify_team'`; on 'no', no signal is written. | pre-yes: `conversation.pendingSignal !== null && conversation.status === 'awaiting_notify_decision'`; post-yes: `getProductSignals().filter(s => s.status === 'provisional' && s.provenance === 'conversational_notify_team').length === 1`; direct call to the signal-write layer without the dual guard (`status === 'awaiting_notify_decision'` + recorded user 'yes') is rejected (RED-3 invariant). | HAPPY-12, D6, N9 |

Post-Cycle-1 distribution: 23 (prior) + 5 (S1 Cycle 1) = 28 total. The 5 new cases map to new spec §5 rules A17 / A18 / D6, the A2 capability-grounding amendment, and the N6 clarification; they are bound to spec §3 FAQ rows HAPPY-3 / HAPPY-11 / HAPPY-12 (multi-case rows). HAPPY-14..18 promote to live-agent tests in Cycle 3 per the architectural plan.
