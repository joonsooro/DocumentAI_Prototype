# evals.md — Document AI Self-Improvement Flywheel (S1)

Binary pass/fail cases harvested 1:1 from `app/spec.html` §3. 12 rows: 6 HAPPY · 4 EDGE · 2 RED.

No 1–5 scoring. No LLM-as-judge. Assertions check shape, presence, and refusal patterns — not exact agent response text.

| Case ID | Scenario | Expected output | Assertion | FAQ source |
|---|---|---|---|---|
| HAPPY-1 | A new operator opens the prototype with no prior context. | Customer Workspace renders the admin-facing intent form and a labelled `CustomerIntent` panel; no end-user upload affordance is present. | `screen.getByLabelText(/customer intent/i)` exists; no `data-testid="end-user-upload"` element rendered; intent flow targets `role=admin`. | HAPPY-1 |
| HAPPY-2 | Admin pastes the DAEJOO prose requirement (supplier, PO, HS code, payment terms, payable amount, exclude no-commercial-value sample lines). | Live AI Core compile produces a `CompiledConfiguration` with all 9 schema fields, field instructions, validation rules, regex patterns, confidence thresholds, processing mode. | `result.schema.fields.length === 9`; every field has non-empty `instruction`, `validation`, `regex`, `confidenceThreshold`; `result.processingMode` is set. | HAPPY-2, HAPPY-3 |
| HAPPY-3 | Same DAEJOO prose; observe agent type for the compile step. | Compile result is tagged as a live `aiCore` agent call, not a template. | `result.source === 'aiCore'`; `result.templateUsed === false`; `QualityMetric` log includes one `aiCore.compile` entry. | HAPPY-3 |
| HAPPY-4 | Admin reviews capability assessment for the DAEJOO requirement set. | Supplier/PO/invoice date → `Supported`; no-commercial-value exclusion → `Supported with workaround`; material-disposal phrase never appears in this view; that phrase appears in Internal as `unsupported_free_text_business_condition`. | Customer Workspace HTML contains no string `Unsupported`; no string `material disposal`; Internal Workspace contains a `ProductSignal` whose `signal_type === 'unsupported_free_text_business_condition'`. | HAPPY-4, RED-2 |
| HAPPY-5 | Admin opens readiness panel after the DAEJOO run. | Readiness panel shows business-language reasons (field, evidence, rule/config, confidence, recommended next action); no raw prompt text; no raw model output. | every reason object has the 5 keys `field, evidence, rule, confidence, nextAction`; panel HTML does not contain the substrings `system:` or `prompt:` or `<|`. | HAPPY-5 |
| HAPPY-6 | Reviewer inspects the three-workspace separation. | Customer / Admin / Internal exist as distinct routes; each reads the same domain objects but renders its own concern. | three routes mount; shared `useDomainStore()` returns the same root object reference in all three; no workspace renders another's data-testid. | HAPPY-6 |
| EDGE-1 | DAEJOO run completes but `payable amount` is missing from extraction. | A `ClarificationRequest` is generated asking field meaning, posting/review/reporting impact, and supplier-scope applicability; no default value substituted. | `clarifications.length >= 1`; the request for `payable_amount` has the 3 prompts; `documentRun.fields.payable_amount` is `null` not a default. | EDGE-1 |
| EDGE-2 | AI Core call for `generateAdminRecommendations` throws / times out / returns invalid JSON. | UI surfaces a `ClarificationRequest` with an operator-facing error; a `QualityMetric` is logged; no canned recommendation appears. | `qualityMetrics` includes `{agent:'admin.recommend', status:'fail'}`; UI shows an error-tagged `ClarificationRequest`; admin recommendation list does not contain any item with `source==='fallback'`. | EDGE-2 |
| EDGE-3 | One operator submits a single correction for a non-recurring DAEJOO field. | No `ProductSignal` is auto-created; the correction sits in the governance queue with frequency/customer-impact/document-type/supplier/country fields pending. | `productSignals` length unchanged after the single correction; `governanceQueue` includes the new `CorrectionEvent` with the 5 governance fields present (some may be unset, none auto-promoted). | EDGE-3 |
| EDGE-4 | Operator walks the full demo (Customer → Admin → Internal) timed end-to-end. | Walkthrough completes in under 5 minutes; all 7 eval families execute against the DAEJOO scenario; adding a second supplier fixture in `src/data/` does not require editing the control layer. | scripted walkthrough Vitest scenario completes with `walkthroughDurationMs < 300_000`; the 7 eval families all report a pass count; a stub Amazon fixture loads without modifying `src/domain/*`. | EDGE-4 |
| RED-1 | Admin opens recommendations after a DAEJOO run that left one field below auto-confirm. | Recommendations list contains schema/prompt/instruction recommendations only; no entry suggests lowering a threshold; readiness for that field stays `Needs review`. | for every `AdminRecommendation r`: `r.type !== 'threshold_lower'` and `r.text` does not match `/lower(ing)?\s+threshold/i`; readiness for the under-confirm field is `Needs review`, not `Ready`. | RED-1 |
| RED-2 | Stakeholder asks the demo to "tell the customer the material-disposal phrase is unsupported." | Customer Workspace continues to show only `Supported` / `Supported with workaround` and the clarification loop; the material-disposal phrase remains only inside Internal Product Intelligence. | full-DOM text snapshot of Customer Workspace does not contain `Unsupported`, `material disposal`, or `unsupported_free_text_business_condition`; same strings DO appear inside Internal Workspace DOM. | RED-2 |

Distribution check: 6 HAPPY (HAPPY-1…6) · 4 EDGE (EDGE-1…4) · 2 RED (RED-1, RED-2) = 12 total, 1:1 with spec §3.
